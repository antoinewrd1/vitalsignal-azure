name: vitalsignal-train
channels: [conda-forge]
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas==3.0.2
      - scikit-learn==1.8.0
      - pyarrow
      - mlflow-skinny==3.15.1
      - azureml-mlflow
      - adlfs                # pandas reads abfss:// through fsspec with this
      - pydantic>=2.7
